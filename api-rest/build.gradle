apply plugin: "org.springframework.boot"

springBoot {
    executable = false
    backupSource = false
}

bootRepackage {
    enabled = false
}

dependencies {
//  Internal Dependencies
    compile project(':api-services')

}
